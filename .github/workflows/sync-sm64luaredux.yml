name: Sync SM64LuaRedux

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    uses: mupen64/.github/.github/workflows/sync-source-folder.yml@main
    with:
      repo: mupen64/SM64LuaRedux
      branch: main
      folder: SM64LuaRedux
      state_file: .github/sync-state/SM64LuaRedux.txt
    secrets: inherit
